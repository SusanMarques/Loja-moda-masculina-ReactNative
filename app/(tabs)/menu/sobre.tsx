import { View, Text, StyleSheet, FlatList, ScrollView } from "react-native";

export default function Screen(){
  
    return(
        <View >
            <ScrollView>
                <Text>Sobre</Text>
                
            </ScrollView>
        </View>
    );
}
